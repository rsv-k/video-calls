import { AppShell } from '@mantine/core';
import { Navbar } from './components';

export const Home = () => {
	return (
		<AppShell
			navbar={<Navbar />}
			padding="xs"
		>
			456
		</AppShell>
	);
};
